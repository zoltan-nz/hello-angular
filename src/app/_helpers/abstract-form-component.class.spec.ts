import AbstractFormComponentClass from './abstract-form-component.class';

describe('AbstractFormComponentComponent', () => {
  it('should create', () => {
    const instance = new AbstractFormComponentClass();
    expect(instance).toBeDefined();
  });
});
